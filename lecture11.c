#include<stdio.h>
void main(){

    // *****Quize--> input rate and qty through key board and find out amount. also find out 10% discount on amount and net amount.
    // float r,q,amt,dis,na;
    // printf("Enter the rate: ");
    // scanf("%f",&r);

    // printf("Enter the quantity: ");
    // scanf("%f",&q);

    // amt=r*q;
    // printf("Total amount is: %.2f\n",amt);

    // dis=(amt*10)/100;
    // printf("Discount: %.2f\n",dis);

    // na=amt-dis;
    // printf("Net amount is: %.2f",na);

    // ****Quize --> input salary through keyboard and find out annual salary, also find out 20% tax on annual salary and the net annual salary.

    // float s, as, tax, nas;

    // printf("salary per month: ");
    // scanf("%f",&s);

    // as=s*12;
    // printf("Annual salary is: %.2f\n",as);

    // tax=(as*20)/100;
    // printf("Tax amount is: %.2f\n",tax);

    // nas=as-tax;
    // printf("Net annual salary is: %.2f\n",nas);

    //***Quize--> input any number thru keyboard and findout its square and cube.  */

    float n,s,c;
    printf("Enter number: ");
    scanf("%f",&n);

    s=n*n;
    printf("Square: %.2f\n",s);

    c=s*n;
    printf("Cube: %.2f\n",c);

}